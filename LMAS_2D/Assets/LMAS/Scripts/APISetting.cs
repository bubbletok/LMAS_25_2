using UnityEngine;

namespace LMAS
{
    public static class APISetting
    {
        public const string IP = "127.0.0.1";
        public const string Port = "8000";
        public const string APIUrl = "http://" + IP + ":" + Port;

    }
}
